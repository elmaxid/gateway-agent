# v0.5.2 — Log de implementación y release

**Fecha:** 2026-07-16/17
**Rama:** `feat/v0.5.2-observability` → merge FF a `master`
**Release:** tag `v0.5.2` = `7836e7d` (github + origin) — https://github.com/elmaxid/gateway-agent/releases/tag/v0.5.2
**Suite final:** 292/292, 0 skips
**Plan de origen:** `docs/gateway-capability-router-repair-plan.md` rev 4 — Fases 0+1 (§4, §5, §13.1)

## Qué shippeó (16 commits)

| Commit | Contenido |
|---|---|
| 3026671 | Contrato fail-loud en los 6 agents .md + test estático anti-"return nothing" |
| 3c2407c, acff94e | `lib/redaction.mjs`: userMessage/operatorDetail/localLogPath 0600, redact-before-truncate; integraciones en task/dispatch/main catch |
| edbdc9c | `lib/background-launch.mjs`: job persistido ANTES del spawn (§5.1.1), reconciliación de PIDs muertos, fix remediación `@openai/codex` |
| 3a16721, e4b3bde | `version --json` + `lib/version-info.mjs` + `scripts/make-build-info.mjs`; warning kind-incompatible en setup add/set-default/set-task-profile |
| 04cb48c | `scripts/baseline-capture.mjs` standalone para puestos (cero imports del repo, salida sin secretos) |
| f5da6c6 | Bump 0.5.2 (3 manifests) |
| a7b338c | Fixwave 1 (cross-review ronda 1): reconcile en `status <id>`/`result`, errorMessage redactado en runTrackedJob, sanitizeError con secretos, baseline pluginRoot-first, query-regex anclada a URL, test commitSource robusto |
| 8d4daf3 | Provenance git-first (marketplace cache sin .git lee build-info commiteado) |
| 20ca772 | Fixwave 2 (codex gpt-5.5 high): **CRITICAL** `setup list --json` filtraba apiKey/authToken → booleanos; setup test/models redactados; operatorDetail acotado; guard GIT_DIR/repo-padre (`ls-files --error-unmatch`); baseline alineado |
| ffe0a1b | Fixwave 3a (dogfood S11): ruta de fallo codex — settle en `close` (no `exit`), error redactado + log 0600, 5/5 runs verificados |
| 0102b56 | Paridad exit→close en claude/zero; test estático extendido a `skills/**` |
| 67f2b5a | Docs: SKILL.md contrato fail-loud, README v0.5.2 (tests, sección observabilidad, fallback claim, tabla status), marketplace/package triple-harness + 6 agents |
| 7836e7d | build-info.json stamp (registra 67f2b5a — off-by-one documentado en make-build-info) |

## Proceso de calidad

1. **SDD**: 6 tareas, implementador + reviewer por tarea (2 Important cerrados en ciclo).
2. **Cross-review ronda 1** (4 modelos): fable "with fixes", codex gpt-5.4 4 important (confirmados), glm-5.2 y minimax-m3 approve → fixwave 1.
3. **Cross-review ronda 2** (pedido explícito): codex **gpt-5.5 reasoning high** encontró 1 CRITICAL + 4 important que gpt-5.4 no vio → fixwave 2. glm delta approve.
4. **Validación de consumidor** (pedido explícito): dogfood de agente fresco solo-docs (11 escenarios: 10 PASS + S11 runtime-bug) + audit de docs (3 blockers) → fixwave 3.
5. Cada finding verificado contra código real por el controller antes de arreglar; cada fixwave re-revisada; suite corrida por el controller en cada cierre.

## Verificaciones empíricas clave

- `setup list --json` contra config real de 11 profiles: 0 secretos, campos `hasApiKey`/`hasAuthToken`.
- Ruta de fallo codex: 5/5 runs no-vacíos, mensaje 217B redactado + `Full details:`, log 0600 (`stat` verificado).
- Provenance: checkout→`git`, sin-git+build-info→`build-info`, instalación vieja→`unknown` (3 rutas corridas).
- Baseline en puesto dev: 4 instalaciones detectadas, matriz 5 ops, 0 fugas.

## Pendiente post-release (equipo)

- Baseline en 2 puestos restantes (uno que falla, uno limpio): `node baseline-capture.mjs --run-matrix --plugin-root <instalación activa>`.
- Update del plugin en puestos: `/plugin marketplace update` + `/reload-plugins`; comparar `gateway-companion version --json` entre puestos.
- Staff: decisiones #15 (schema v2), #4 (capabilities), #13 (aislamiento) → habilitan Etapa B (v0.6.0 capability router).

## Deuda diferida a v0.6 (ticket único)

Foreground progress: threading de secretos + relay crudo de stderr de harness (accepted-risk dictaminado); consolidar persistFailed/persistWorkerLaunchFailure + tests faltantes (EPERM/log-append/async-error); rotación de error-logs; `starting` en filtros de activos; make-build-info root guard; safeSection shape en baseline; reap de queued-con-PID-vivo; heartbeat anti PID-reuse; redactText O(n²) en líneas >10K sin `://`; zero-init imprime baseUrl crudo; userinfo regex con `@` crudo en password; consistencia baseUrl en setup list.

## Lecciones (ver `tasks/lessons.md` 2026-07-17)

- Settle de child process en `close`, nunca en `exit` (pierde output).
- Tests estáticos de contrato deben cubrir `agents/` **y** `skills/`.
- Dogfood de consumidor encuentra clases de bugs que los reviews de código no ven.
- Modelo pedido = modelo corrido, o sustitución declarada (gpt-5.5 high encontró lo que gpt-5.4 no).
- Marketplace cache no tiene `.git`: provenance viaja en build-info.json commiteado en el release; precedencia git-first protege checkouts dev.
